name: mobile
description: Nexthria live-painting retinal mosaicking mobile shell.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.2+1
  image: ^4.5.4
  nexthria_cv:
    path: ../../packages/nexthria_cv
  nexthria_domain:
    path: ../../packages/nexthria_domain
  nexthria_ui:
    path: ../../packages/nexthria_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/demo_sequences/
