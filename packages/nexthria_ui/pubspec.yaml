name: nexthria_ui
description: Shared UI primitives for Nexthria capture, guidance, and status instrumentation.
version: 0.1.0

environment:
  sdk: ^3.11.1
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  nexthria_domain:
    path: ../nexthria_domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
