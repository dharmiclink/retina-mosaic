# Run with `dart run ffigen --config ffigen.yaml`.
name: NexthriaCvBindings
description: |
  Bindings for `src/include/nexthria_cv.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/nexthria_cv_bindings_generated.dart'
headers:
  entry-points:
    - 'src/include/nexthria_cv.h'
  include-directives:
    - 'src/include/nexthria_cv.h'
comments:
  style: any
  length: full
