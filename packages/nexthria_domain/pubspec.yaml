name: nexthria_domain
description: Shared contracts for Nexthria capture, guidance, and export flows.
version: 0.1.0

environment:
  sdk: ^3.11.1
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
